Table "membership_riddle_nouns" {
  "noun" "character varying(20)" [pk, not null]
}

Table "membership_riddle_traits" {
  "trait" "character varying(20)" [pk, not null]
}

Table "messages" {
  "id" integer [pk, not null]
  "title" "character varying(50)" [not null]
  "text" "character varying(2000)" [not null]
  "creation_date" timestamp [not null, default: `CURRENT_TIMESTAMP`]
  "users_only" boolean [not null, default: false]
  "members_only" boolean [not null, default: false]
  "author_id" integer [not null]
}

Table "session" {
  "sid" "character varying" [pk, not null]
  "sess" json [not null]
  "expire" timestamp(6) [not null]

  Indexes {
    expire [type: btree, name: "IDX_session_expire"]
  }
}

Table "users" {
  "id" integer [pk, not null]
  "username" "character varying(20)" [unique, not null]
  "password_hash" "character varying(255)" [not null]
  "name" "character varying(32)" [not null]
  "surname" "character varying(32)" [not null]
  "signup_date" timestamp [not null, default: `CURRENT_TIMESTAMP`]
  "membership_start_date" timestamp
  "admin_start_date" timestamp
  "is_member" boolean [not null, default: false]
  "is_admin" boolean [not null, default: false]
  "membership_trait_noun" "character varying(41)" [unique]
}

Ref "messages_author_id_fkey":"users"."id" < "messages"."author_id"
